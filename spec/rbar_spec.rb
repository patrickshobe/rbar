# frozen_string_literal: true

RSpec.describe Rbar do
  it "has a version number" do
    expect(Rbar::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
